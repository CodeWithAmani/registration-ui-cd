apiVersion: apps/v1
kind: Deployment
metadata:
  name: registration-ui:1.0.0-12
spec:
  replicas: 2
  selector:
    matchLabels:
      app: registration-ui:1.0.0-12
  template:
    metadata:
      labels:
        app: registration-ui:1.0.0-12
    spec:
      containers:
        - name: registration-ui:1.0.0-12
          image: abeldock/registration-ui:1.0.0-12
          resources:
            limits:
              memory: "256Mi"
              cpu: "500m"
          ports:
            - containerPort: 8080
